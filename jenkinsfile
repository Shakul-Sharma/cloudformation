pipeline {
    agent any
    environment {
  enviroment = "dev, qa , prod"
}
    stages {
        stage('Create Stack') {
            steps {
            sh "aws cloudformation create-stack --stack-name s3bucket3 --template-body file://s3cft.json --region 'us-east-1'"
              }
             }
            }
            }
